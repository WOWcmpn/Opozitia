/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["static.mk.ru", "localhost", "picsum.photos", 'plus.unsplash.com'], // <== Domain name
  },
};

export default nextConfig;
